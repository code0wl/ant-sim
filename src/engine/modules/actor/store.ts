export const actorStore = new Set();
export const cellStore = new Set();

export const controls = {
    debug: false,
};

export const foodStores: { [key: string]: number } = {
    black: 0,
    red: 0,
};
